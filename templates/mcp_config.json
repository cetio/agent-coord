{
  "comment": "Copy into <workspace>/.devin/mcp_config.local.json and replace {{COORD_ROOT}} with this clone's absolute path. A single Ruby stdio server maps Devin session IDs to profiles through the PreToolUse hook.",
  "mcpServers": {
    "agent-coord": {
      "command": "ruby",
      "args": ["{{COORD_ROOT}}/source/core/server.rb"]
    }
  }
}
