{
  "comment": "Workspace coordination config — copy into <workspace>/.devin/coord.json and fill in the {{…}} placeholders. The hooks and the Team Room extension read this; nothing else should need to. `roster` is the expected team — agent names, which ARE the identities; there are no seats. `coordRoot` is this clone's absolute path: the extension loads the bus store and the shared room actions from it.",
  "project": "{{PROJECT}}",
  "human": "{{HUMAN}}",
  "teamRoom": "general",
  "roster": [],
  "teamSkill": "{{PROJECT}}-team",
  "recessSkill": "{{PROJECT}}-recess",
  "coordRoot": "{{COORD_ROOT}}"
}
