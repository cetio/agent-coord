{
  "comment": "Workspace coordination config — generated by `coord init`, editable after. The hooks read this; nothing else should need to.",
  "project": "{{PROJECT}}",
  "port": 0,
  "human": "{{HUMAN}}",
  "teamRoom": "general",
  "seats": {},
  "teamSkill": "{{PROJECT}}-team",
  "recessSkill": "{{PROJECT}}-recess",
  "coordRoot": "{{CANONICAL_ROOT}}"
}
