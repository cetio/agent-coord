{
  "comment": "Workspace coordination config — copy into <workspace>/.devin/coord.json and fill in the {{…}} placeholders. The hooks read this; nothing else should need to. `roster` is the expected team — agent names, which ARE the identities; there are no seats. `port` of null auto-allocates into the clone's ports.json on first `chat start`.",
  "project": "{{PROJECT}}",
  "port": null,
  "human": "{{HUMAN}}",
  "teamRoom": "general",
  "roster": [],
  "teamSkill": "{{PROJECT}}-team",
  "recessSkill": "{{PROJECT}}-recess",
  "coordRoot": "{{COORD_ROOT}}"
}
