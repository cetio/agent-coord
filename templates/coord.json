{
  "comment": "Workspace coordination config — generated by `coord init`, editable after. The hooks read this; nothing else should need to. `roster` is the expected team — agent names, which ARE the identities; there are no seats.",
  "project": "{{PROJECT}}",
  "port": 0,
  "human": "{{HUMAN}}",
  "teamRoom": "general",
  "roster": [],
  "teamSkill": "{{PROJECT}}-team",
  "recessSkill": "{{PROJECT}}-recess",
  "coordRoot": "{{CANONICAL_ROOT}}"
}
